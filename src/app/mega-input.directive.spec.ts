import { MegaInputDirective } from './mega-input.directive';

describe('MegaInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MegaInputDirective();
    expect(directive).toBeTruthy();
  });
});
